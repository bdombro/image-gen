[project]
name = "mflux2-gen"
version = "0.1.0"
description = "FLUX image generation using mflux"
requires-python = ">=3.12"
dependencies = [
    "mflux>=0.17",
    "mlx>=0.23",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.uv]
python-preference = "only-managed"
